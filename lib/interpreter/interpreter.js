var Protoplast = require('protoplast');

/**
 * @exports Interpreter
 */
var Interpreter =  Protoplast.extend({

    /**
     * @method
     * @param {Tokens[]}
     * @returns Blueprint
     */
    process: null

});

module.exports = Interpreter;